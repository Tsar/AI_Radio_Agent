# Шпаргалка запуска

Команды для локального прогона на dev-машине — в конфигурации, максимально близкой
к продовой. Установка описана в [README](README.md), развёртывание на даче —
в [DEPLOY.md](DEPLOY.md).

Нужны три процесса: LLM, преобразование голоса и сам агент. Первые два — в отдельных
терминалах, они работают, пока нужны.

На dev-машине монитор подключён к той же 2070, и десктоп забирает ~2.5 ГБ из восьми —
подробности в разделе «Видеопамять». Контекст LLM здесь поэтому ужат. А вот RVC на
процессор выносить больше не нужно: после перехода на `large-v3-turbo` и `f0_method=pm`
весь стек занимает 3.9 ГБ и помещается на видеокарте.

## 1. LLM (llama-server)

Из корня проекта:

```bash
../llama.cpp/build/bin/llama-server -m models/llm/Qwen3-4B-Q4_K_M.gguf \
    -ngl 99 -c 2048 --cache-type-k q8_0 --cache-type-v q8_0 \
    --host 127.0.0.1 --port 8080
```

Контекста 2048 хватает: в истории диалога максимум 8 коротких реплик.

## 2. Преобразование голоса (RVC)

```bash
cd ../Retrieval-based-Voice-Conversion-WebUI
.venv/bin/python infer-http-service.py --port 8081
```

Занимает 592 МБ VRAM: F0 считается методом `pm` на процессоре, нейросетевой `rmvpe`
не грузится. Если памяти совсем нет, RVC можно целиком увести на CPU, добавив
`CUDA_VISIBLE_DEVICES=` — но тогда конвертация займёт 0.6–0.7 с вместо 0.35.

Здесь карта Turing, поэтому сервис идёт в fp16 и берёт свою раскладку нарезки. Чтобы
воспроизвести прод (Pascal, fp32, 5 ГБ) — те же флаги, что стоят в юните на даче:

```bash
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
    .venv/bin/python infer-http-service.py --port 8081 --fp32 --max-segment 6
```

Проверить, что доехало: `curl -s 127.0.0.1:8081/health` → `"is_half": false` и
`"chunking": {"x_pad": 1, ..., "x_max": 9}`. Почему это важно — DEPLOY.md, раздел 8.

## 3. Агент

Дождитесь, пока поднимутся оба сервиса:

```bash
curl -s http://127.0.0.1:8080/health    # {"status":"ok"}
curl -s http://127.0.0.1:8081/health    # {"status":"ok","voice":"voicevox_speaker_43",...}
```

Затем из корня проекта:

```bash
.venv/bin/python main.py run --live --responder llm --rvc \
    --profile vram5 --threshold 0.00067
```

Скажите «**Феечка**, как слышно?» → пауза около секунды → ответ. Следующие 60 секунд
позывной не нужен, «отбой» закрывает диалог. Остановка — `Ctrl+C`.

## Что стоит знать

- **Наушники, а не колонки** — иначе агент услышит собственный ответ. PTT здесь
  заглушка (`dummy`), в эфир ничего не уходит.
- **Порог `--threshold` свой на каждой установке.** Значение выше снято с микрофона
  dev-машины; своё получите командой
  `main.py calibrate --live --seconds 12`.
- **Если агент стартовал раньше сервисов** — он не падает, а пишет `[warn]`: без
  llama-server молчит, без RVC передаёт голосом Piper.
- **Первый запуск `--profile vram5` дольше**: `large-v3-turbo` качается с HuggingFace
  (1.6 ГБ), дальше грузится из кэша за несколько секунд, плюс пара секунд на прогрев
  при первой фразе.
- Для быстрых итераций, когда проверяете механику, а не качество распознавания, —
  `--profile small` (модель `small`, грузится за пару секунд).

## Видеопамять

Замерено (пик, `int8`):

| Что | VRAM |
|---|---|
| десктоп: gnome-shell, Telegram, Element, Chrome, терминал | **2.5 ГБ** |
| llama-server, `-c 4096` | 3.1 ГБ |
| llama-server, `-c 2048` + KV `q8_0` | **2.7 ГБ** |
| Whisper `large-v3-turbo` (`vram5`) | 1231 МБ |
| Whisper `large-v3` (`vram10`) | 2027 МБ |
| Whisper `small` (профиль `small`) | 465 МБ |
| RVC fp32, `f0_method=pm` | 592 МБ |
| RVC fp32, `f0_method=rmvpe` | 926 МБ |

Весь стек с профилем `vram5` — **3.9 ГБ**, плюс 2.5 ГБ десктопа даёт 6.4 из 8. Влезает.

Главное, что легко упустить: **десктоп занимает 2.5 ГБ**, потому что монитор на
dev-машине подключён к той же 2070. Пока стек был на `large-v3` и `-c 4096`, это
приводило к `CUDA failed with error out of memory` — причём **не при старте, а на
первой транскрипции**: инициализация проходила, а буферов энкодера уже не хватало.

Если захотите `--profile vram10` (полная `large-v3`) — добавится ещё 800 МБ, и станет
тесно; закрыть Telegram, Element и Chrome освободит около 450 МБ.

## Чем это отличается от прода

Всё в [DEPLOY.md](DEPLOY.md), здесь коротко:

| | Локально | Дачная (P2000, 5 ГБ) | Домашняя (P102-100, 10 ГБ) |
|---|---|---|---|
| PTT | заглушка | `--ptt txdbreak --port /dev/ttyUSB0` | — |
| Звуковая карта | системный дефолт | `--device-rate 48000` | — |
| Профиль STT | `vram5` | `vram5` (turbo) | `vram10` (large-v3) |
| Точность RVC | fp16 | fp32, **только флагом `--fp32`** | fp32, переключается сам |
| Контекст LLM | `-c 2048` + KV `q8_0` | `-c 2048` + KV `q8_0` | `-c 4096`, памяти хватает |

То есть боевая команда выглядит так:

```bash
.venv/bin/python main.py run --live --responder llm --rvc \
    --profile vram5 --device-rate 48000 --threshold ПОРОГ \
    --ptt txdbreak --port /dev/ttyUSB0
```

На проде всё это поднимается systemd-юнитами, вручную запускать не придётся —
см. раздел «Автозапуск» в DEPLOY.md.

## Замер локальной конфигурации

Синтетический эфир (три передачи, полоса 300–3400 Гц), `--profile vram5 --rvc` —
для сверки, если что-то станет заметно медленнее:

```
  #   приём   STT     LLM     TTS    итого   эфир  ответ
  1    2.46    0.33    0.21    0.37    0.93    1.53   да
  2    1.78    0.22    0.14    0.44    0.80    2.15   да
  3    3.08    0.23    0.14    0.30    0.67    1.83   да
  бюджет 10 с — укладываемся (худшая фраза 0.93 с)
```

В колонке TTS — Piper вместе с RVC. На дачной машине ожидается примерно вдвое
больше: P2000 медленнее 2070. Сам Piper при этом не проседает — замер на i7-2600
дал 0.41–0.43 с на фразу (RTF 0.10–0.17), отсутствие AVX2 ему не мешает.
