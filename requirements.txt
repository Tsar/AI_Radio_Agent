# Этап 1 (калибровка VAD по файлу) зависит только от stdlib + системного ffmpeg —
# pip-пакеты ниже понадобятся начиная с живого аудио (микрофон/динамик) и PTT.
#
# Системные зависимости (ставятся не через pip):
#   ffmpeg            — декодирование/ресемплинг аудиофайлов
#   libportaudio2     — бэкенд для sounddevice (Linux)
#
numpy
sounddevice
pyserial
